<% var meta = {description : 'test',title : 'gulp'}; %>
<% include _template %>
<% function contents() { %>

    <section>
      <p>sample</p>
      <div>
        <img src="<%= config.imgpath %>/miffy.jpg" alt="">
      </div>
    </section>

<% } %>